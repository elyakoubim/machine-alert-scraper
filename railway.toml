# railway.toml — Déploiement Railway

[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/"
restartPolicyType = "on_failure"

[[services]]
name = "machine-alert-scraper"
